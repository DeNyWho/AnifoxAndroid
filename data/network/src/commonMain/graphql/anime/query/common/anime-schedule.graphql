query AnimeSchedules($dayOfWeek: DayOfWeek!) {
  animeSchedules(dayOfWeek: $dayOfWeek) {
    dayOfWeek
    nextEpisodeDate
    previousEpisodeDate
    animeId
    anime {
      ...AnimeLite
    }
  }
}